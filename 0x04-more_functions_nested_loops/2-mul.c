#include "main.h"

/**
 * mul- function that checks for a dgit (0 though)
 * @a: is a variable
 * @b: is a variable
 * Return: Always 0
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
