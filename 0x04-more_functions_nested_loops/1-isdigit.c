#include "main.h"

/**
 * _isdigit- Finction checks for a digit (0 through 9)
 * @c: Integer to be checked
 * Return: 1 if c is digit, 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
